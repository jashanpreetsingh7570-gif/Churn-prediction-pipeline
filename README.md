# Churn-prediction-pipeline
Customer churn prediction pipeline using scikit-learn (Logistic Regression, Random Forest, Gradient Boosting) with GridSearchCV tuning, feature importance analysis, and evaluation via confusion matrix, ROC-AUC, and precision-recall curves. Trained on the Telco Customer Churn dataset.
